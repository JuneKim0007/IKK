{
  "name": "ikk-web",
  "private": true,
  "type": "module",
  "description": "IKK web editor. Native ES modules, zero runtime dependencies.",
  "scripts": {
    "typecheck": "tsc --noEmit -p jsconfig.json",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=18" }
}
