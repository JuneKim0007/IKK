package com.ikk.crypto

/** The operation categories the benchmark covers. */
enum class OperationCategory {
    KEY_GENERATION,
    SYMMETRIC_ENCRYPTION,
    HASHING,
    MESSAGE_AUTHENTICATION,
    ASYMMETRIC_SIGNING,
}
